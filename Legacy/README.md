# UEBridgeMCP Legacy Sources

`Nwiro/` contains the older UE_MCP_Bridge handler set kept for reference while the
active plugin uses the WorldData MCP tool modules under `Source/UEBridgeMCPEditor`.

The legacy tree is intentionally outside `Source/` so UnrealBuildTool does not
compile it into `UEBridgeMCPEditor`.
